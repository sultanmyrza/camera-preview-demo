import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'camera-preview-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
